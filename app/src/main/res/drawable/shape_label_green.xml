<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 实心 -->
    <solid android:color="@color/label_light_green"/>
    <!-- 边框 -->
    <stroke
        android:width="1px"
        android:color="@color/label_mid_green"/>
    <!-- 圆角 -->
    <!--<corners android:radius="3dp"/>-->
    <!-- 边距 -->
    <padding
        android:top="1dp"
        android:bottom="1dp"
        android:left="3dp"
        android:right="3dp"/>
</shape>